def add(x):
    return x + 5

a = 10
b = add(a)

print(a)
print(b)

#このコードは、関数addを定義し、引数xに5を加えた値を返すものである。変数aに10を代入し、bにはadd関数を呼び出してaの値を渡しています。その後、aとbの値を出力している。
#addとは、引数として渡された値に数字を加える関数である。
#defとは、Pythonにおいて関数を定義するためのキーワードである。